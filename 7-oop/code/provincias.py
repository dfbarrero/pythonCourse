provincias = 40
while provincias <= 50:
	print "El numero de provincias es:", str(provincias) #Metodo que transforma provincias a cadena.
	provincias += 1 #Esto suma una provincia al atributo provincias.
	
for provincias in range(45,50):
	print "la provincia es la numero:",str(provincias)